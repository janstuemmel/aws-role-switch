export {
  setConfig,
  getConfig,
} from './storage';

export { parseConfig } from './parse';

export { mapConfig } from './mapper';

export { 
  decompressConfig, 
  compressConfig,
} from './gzip';
